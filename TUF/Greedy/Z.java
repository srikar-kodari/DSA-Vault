public class Z {
    public static void main(String[] args) {

        // int[][] matrix = {{3,0,8,4},{2,4,5,7},{9,2,6,3},{0,3,1,0}};
        // int n = matrix.length;
        // int[] rowMax = new int[n];

        // for(int i=0; i<n; i++) {
        //     rowMax[i] = Arrays.stream(matrix[i]).max().orElse(Integer.MIN_VALUE);
        // }

        // System.out.println(Arrays.toString(rowMax));

        // StringBuilder sb = new StringBuilder(Integer.toString(24));
        // System.out.println(sb);

        char ch = 2;
        System.out.println(ch + '0');

    }
}
